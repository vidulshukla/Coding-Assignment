#include<stdio.h>
#include<conio.h>
void main()
{
  int arr[20],i,n,j=0,temp=[20];
  clrscr();
  printf("Enter the number of elements: ");
  scanf("%d",&n);
  for("i=0;i<n;i++)
    {
      printf("%d",arr[i]);
    }
    getch();
 }
